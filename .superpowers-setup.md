# Superpowers Setup for Hail Project

## Overview

Superpowers is a complete software development methodology for coding agents, built on composable skills that auto-trigger at the right moments. The framework has been cloned to `/tmp/agent_4ee0f43a-9385-46fd-aa1a-81e54d40b5d3/skills/superpowers`.

## Activation in This Project

### For Kilo (Current Harness)

Since Kilo uses a skill-based system similar to OpenCode, Superpowers skills can be loaded using Kilo's `skill` tool. The skills are located at:

```
/tmp/agent_4ee0f43a-9385-46fd-aa1a-81e54d40b5d3/skills/superpowers/skills/
```

### Manual Activation

To use a specific skill in Kilo:

```bash
# Use the skill tool to load a skill
skill brainstorming
skill writing-plans
skill systematic-debugging
# etc.
```

### Auto-Trigger Behavior

The `using-superpowers` skill is the bootstrap that causes skills to auto-trigger. In Kilo, you need to explicitly invoke it at session start or include its content in your AGENTS.md / CLAUDE.md / kilo.json instructions.

**Recommended**: Add the `using-superpowers` skill content to your project's AGENTS.md or create a `.kilo/agent/superpowers.md` agent file that includes the bootstrap instructions.

## Available Skills

| Skill | Purpose | When to Use |
|-------|---------|-------------|
| **brainstorming** | Socratic design refinement - explores intent, requirements, design before implementation | Before ANY creative work: new features, components, functionality, behavior changes |
| **writing-plans** | Detailed implementation plans with bite-sized tasks (2-5 min each) | After approved design/spec, before touching code |
| **subagent-driven-development** | Execute plans via fresh subagent per task with two-stage review | When executing implementation plans with independent tasks in current session |
| **executing-plans** | Batch execution with human checkpoints | Alternative to subagent-driven for parallel sessions |
| **test-driven-development** | RED-GREEN-REFACTOR cycle enforcement | During implementation (always active when writing code) |
| **systematic-debugging** | 4-phase root cause process (investigation → pattern → hypothesis → fix) | When encountering ANY bug, test failure, or unexpected behavior |
| **verification-before-completion** | Evidence before claims - run verification commands before declaring success | Before ANY completion claim, commit, PR, or task completion |
| **dispatching-parallel-agents** | Concurrent subagent workflows | When tasks can run in parallel |
| **requesting-code-review** | Pre-review checklist and review dispatch | Between tasks, before finishing branch |
| **receiving-code-review** | Responding to feedback | When receiving code review feedback |
| **using-git-worktrees** | Isolated workspaces on new branches | After design approval, before implementation |
| **finishing-a-development-branch** | Merge/PR decision workflow, cleanup | When all tasks complete |
| **using-superpowers** | Bootstrap - establishes skill usage rules | **ALWAYS at session start** |
| **writing-skills** | Create new skills following best practices | When creating custom skills |

## How to Invoke Each Skill

### In Kilo

Use the `skill` tool:

```
skill brainstorming
skill writing-plans
skill systematic-debugging
skill verification-before-completion
skill subagent-driven-development
skill executing-plans
skill test-driven-development
skill dispatching-parallel-agents
skill requesting-code-review
skill receiving-code-review
skill using-git-worktrees
skill finishing-a-development-branch
skill using-superpowers
skill writing-skills
```

### Skill Invocation Rules (from using-superpowers)

1. **ALWAYS invoke relevant skills BEFORE any response or action** - including clarifying questions
2. **Before entering plan mode**: if you haven't brainstormed, invoke brainstorming first
3. **Announce**: "Using [skill] to [purpose]" and follow the skill exactly
4. **If skill has a checklist**: create a todo per item

### Skill Priority

Process skills come first (they set the approach), then implementation skills carry it out:
- "Let's build X" → **brainstorming** first, then implementation skills
- "Fix this bug" → **systematic-debugging** first, then domain skills

## Project-Specific Configuration

### Hail Project Details

- **Project Type**: Android/Gradle (Kotlin/Java)
- **Root**: `/workspace/d031c350-535d-4160-8cd6-9e3e237967bd/sessions/agent_4ee0f43a-9385-46fd-aa1a-81e54d40b5d3/`
- **Build**: `./gradlew` commands
- **Tests**: Likely JUnit/Espresso (check `app/src/test` and `app/src/androidTest`)

### Recommended Setup

1. **Create AGENTS.md** in project root with Superpowers bootstrap:

```markdown
# Hail Project - Agent Instructions

## Superpowers Bootstrap

<EXTREMELY_IMPORTANT>
You have superpowers. The using-superpowers skill content is included below. It is ALREADY LOADED - you are currently following it. Do NOT use the skill tool to load "using-superpowers" again.

[Include content from /tmp/agent_4ee0f43a-9385-46fd-aa1a-81e54d40b5d3/skills/superpowers/skills/using-superpowers/SKILL.md]

**Tool Mapping for Kilo:**
When skills request actions, substitute Kilo equivalents:
- Create or update todos → `todowrite`
- Subagent (general-purpose) → `task` tool with appropriate subagent type
- Invoke a skill → Kilo's native `skill` tool
- Read files → `read`
- Create, edit, or delete files → `edit` / `write`
- Run shell commands → `bash`
- Search files → `grep`, `glob`
- Fetch a URL → `webfetch`
</EXTREMELY_IMPORTANT>
```

2. **Add skills path to kilo.json** (if Kilo supports skills.paths config):

```json
{
  "skills": {
    "paths": ["/tmp/agent_4ee0f43a-9385-46fd-aa1a-81e54d40b5d3/skills/superpowers/skills"]
  }
}
```

3. **Spec/Plan Directories** (created automatically by skills):
- Specs: `docs/superpowers/specs/YYYY-MM-DD-<topic>-design.md`
- Plans: `docs/superpowers/plans/YYYY-MM-DD-<feature-name>.md`

## The Basic Workflow

1. **brainstorming** → Refines rough ideas through questions, explores alternatives, presents design in sections for validation. Saves design document.

2. **using-git-worktrees** → Creates isolated workspace on new branch, runs project setup (`./gradlew`), verifies clean test baseline.

3. **writing-plans** → Breaks work into bite-sized tasks (2-5 minutes each). Every task has exact file paths, complete code, verification steps.

4. **subagent-driven-development** (or **executing-plans**) → Dispatches fresh subagent per task with two-stage review (spec compliance, then code quality), or executes in batches with human checkpoints.

5. **test-driven-development** → Enforces RED-GREEN-REFACTOR: write failing test, watch it fail, write minimal code, watch it pass, commit.

6. **requesting-code-review** → Reviews against plan, reports issues by severity. Critical issues block progress.

7. **finishing-a-development-branch** → Verifies tests, presents options (merge/PR/keep/discard), cleans up worktree.

## Key Principles

- **Test-Driven Development** - Write tests first, always
- **Systematic over ad-hoc** - Process over guessing
- **Complexity reduction** - Simplicity as primary goal
- **Evidence over claims** - Verify before declaring success

## Red Flags (STOP and use skills)

| Thought | Reality |
|---------|---------|
| "This is just a simple question" | Questions are tasks. Check for skills. |
| "I need more context first" | Skill check comes BEFORE clarifying questions. |
| "Let me explore the codebase first" | Skills tell you HOW to explore. Check first. |
| "This doesn't need a formal skill" | If a skill exists, use it. |
| "I'll just do this one thing first" | Check BEFORE doing anything. |

## Verification Commands for Hail

Before claiming completion, run:

```bash
# Build verification
./gradlew build

# Test verification
./gradlew test
./gradlew connectedAndroidTest  # if device/emulator available

# Lint verification
./gradlew lint
```

## Troubleshooting

### Skills Not Loading
1. Verify skills directory path: `/tmp/agent_4ee0f43a-9385-46fd-aa1a-81e54d40b5d3/skills/superpowers/skills`
2. Check Kilo skill tool can discover skills
3. Ensure using-superpowers bootstrap is active

### Brainstorming Not Auto-Triggering
- Explicitly invoke: `skill brainstorming`
- Ensure using-superpowers bootstrap is loaded at session start

### Worktree Issues
- Use `skill using-git-worktrees` to create isolated workspace
- Never implement on main/master without explicit consent

## Resources

- Superpowers Repo: `/tmp/agent_4ee0f43a-9385-46fd-aa1a-81e54d40b5d3/skills/superpowers/`
- Skills Directory: `/tmp/agent_4ee0f43a-9385-46fd-aa1a-81e54d40b5d3/skills/superpowers/skills/`
- OpenCode Integration: `/tmp/agent_4ee0f43a-9385-46fd-aa1a-81e54d40b5d3/skills/superpowers/.opencode/`
- Documentation: `/tmp/agent_4ee0f43a-9385-46fd-aa1a-81e54d40b5d3/skills/superpowers/docs/`